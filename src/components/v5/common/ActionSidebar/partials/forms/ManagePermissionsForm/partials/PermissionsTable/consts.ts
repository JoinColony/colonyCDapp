import { defineMessages } from 'react-intl';

export const CUSTOM_PERMISSIONS_TABLE_MSG = defineMessages({
  permissionInherited: {
    id: 'managePermissionsForm.customPermissionsTable.permissionInherited',
    defaultMessage: 'Permission already inherited from a parent team',
  },
});
