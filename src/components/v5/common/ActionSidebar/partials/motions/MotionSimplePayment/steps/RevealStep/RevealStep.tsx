import React, { FC } from 'react';

const displayName =
  'v5.common.ActionSidebar.partials.motions.MotionSimplePayment.steps.RevealStep';

const RevealStep: FC = () => {
  return <div>RevealStep</div>;
};

RevealStep.displayName = displayName;

export default RevealStep;
