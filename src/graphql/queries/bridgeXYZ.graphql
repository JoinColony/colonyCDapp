query BridgeXYZQuery($input: BridgeXYZQueryInput!) {
  bridgeXYZQuery(input: $input) {
    drains {
      id
      amount
      state
      created_at
      deposit_tx_hash
      receipt {
        destination_currency
        url
      }
    }
    success
    transactionFee
  }
}
