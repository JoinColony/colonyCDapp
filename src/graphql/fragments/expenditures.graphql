fragment Expenditure on Expenditure {
  id
  nativeId
  ownerAddress
  status
  slots {
    ...ExpenditureSlot
  }
  nativeFundingPotId
  metadata {
    nativeDomainId
  }
  balances {
    tokenAddress
    amount
    requiredAmount
  }
}

fragment ExpenditureSlot on ExpenditureSlot {
  id
  recipientAddress
  claimDelay
  payoutModifier
  payouts {
    tokenAddress
    amount
  }
}
