fragment ColonyAction on ColonyAction {
  id
  transactionHash: id
  type
  blockNumber
  initiatorAddress
  initiator {
    ...User
  }
  recipientAddress
  recipient {
    ...User
  }
  amount
  decimals
  tokenSymbol
  fromDomain
  toDomain
  createdAt
}
