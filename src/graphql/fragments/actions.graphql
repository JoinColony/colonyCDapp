fragment ColonyAction on ColonyAction {
  id
  type
  blockNumber
  transactionHash
  initiatorAddress
  initiator {
    ...User
  }
  recipientAddress
  recipient {
    ...User
  }
  amount
  decimals
  symbol
  fromDomain
  toDomain
  createdAt
}
