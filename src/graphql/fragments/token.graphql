fragment Token on Token {
  decimals
  tokenAddress: id
  name
  symbol
  type
  avatar
  thumbnail
}

fragment UserTokenBalanceData on GetUserTokenBalanceReturn {
  balance
  lockedBalance
  activeBalance
}
