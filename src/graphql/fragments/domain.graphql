fragment Domain on Domain {
  id
  nativeId
  isRoot
  nativeFundingPotId
  metadata {
    name
    color
    description
    changelog {
      transactionHash
      oldName
      newName
      oldColor
      newColor
      oldDescription
      newDescription
    }
  }
}
