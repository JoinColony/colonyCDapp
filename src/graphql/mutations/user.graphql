mutation CreateUniqueUser($input: CreateUniqueUserInput!) {
  createUniqueUser(input: $input) {
    id
  }
}

mutation UpdateUserProfile($input: UpdateProfileInput!) {
  updateProfile(input: $input) {
    id
    avatar
    bio
    displayName
    location
    website
    email
    notificationSettings {
      notificationTokens
      enableEmail
      enableMention
      enableAssign
    }
  }
}

mutation SendNotification($input: SendNotificationInput!) {
  sendNotification(input: $input)
}

mutation SubscribeToColonyPushNotifications(
  $input: SubscribeColonyPushNotificationInput!
) {
  subscribeToColonyPushNotifications(input: $input)
}

mutation UpdateUserNotificationToken($id: ID!, $notificationTokens: [String]!) {
  updateProfile(
    input: {
      id: $id
      notificationSettings: { notificationTokens: $notificationTokens }
    }
  ) {
    id
    notificationSettings {
      notificationTokens
    }
  }
}
